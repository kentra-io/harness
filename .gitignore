# OS / editor junk
.DS_Store
*.log
.idea/

# External reference repos cloned for analysis (have their own .git — not vendored)
references/conductor/
references/fabro/

# claudebox tool checkout (its own git repo — external clone, not a submodule)
claudebox/

# kafka-dq M9 dogfood testbed (its own nested git repo — not a submodule)
kafka-dq/

# kentra-skills scratch working copy (own git repo; canonical home = kentra-io/kentra-skills)
.claudebox/tmp/kentra-skills/

# Claude Code per-machine local settings (never shared)
.claude/settings.local.json
.worktrees/

# Retro transcript archives (large; working copies for retros, not repo history)
tasks/retro-archive/
